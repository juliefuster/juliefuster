"use client" import { useEffect, useState } from "react" import { Card } from "@/components/ui/card" import { Button } from "@/components/ui/button" import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table" import { ArrowLeft, Calendar, Wrench } from "lucide-react" import Link from "next/link" import { useParams } from "next/navigation" interface PumpRecord { mes: string fecha: string numero_bomba: number responsable: string } export default function PumpChangeHistory() { const params = useParams() const hotel = params.hotel as string const hotelName = hotel === "caledonian" ? "Hotel Caledonian" : "Hotel Chi" const [records, setRecords] = useState<PumpRecord[]>([]) const [loading, setLoading] = useState(true) useEffect(() => { async function fetchHistory() { try { const response = await fetch(/api/pump-change/history?hotel=${hotel}) const data = await response.json() setRecords(data) } catch (error) { console.error("Error loading pump change history:", error) } finally { setLoading(false) } } fetchHistory() }, [hotel]) return ( <div className="min-h-screen bg-gradient-to-br from-slate-50 to-slate-100"> <header className="bg-white border-b border-slate-200 shadow-sm"> <div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-6 flex items-center justify-between"> <div className="flex items-center gap-4"> <Link href={/${hotel}/preventivo}> <Button variant="ghost" size="icon"> <ArrowLeft className="h-5 w-5" /> </Button> </Link> <div> <h1 className="text-2xl font-bold text-slate-900 flex items-center gap-2"> <Wrench className="h-6 w-6 text-green-600" /> Historial de Cambios de Bombas </h1> <p className="text-sm text-slate-600">{hotelName}</p> </div> </div> <Button onClick={() => window.print()} variant="outline" className="bg-white hover:bg-slate-50 print:hidden" > <Calendar className="h-4 w-4 mr-2" /> Imprimir </Button> </div> </header> <main className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-10"> {loading ? ( <Card className="p-8 text-center"> <p className="text-slate-600">Cargando historial...</p> </Card> ) : records.length === 0 ? ( <Card className="p-8 text-center"> <p className="text-slate-600">No hay registros de cambio de bombas aún.</p> </Card> ) : ( <Card className="p-6 bg-white shadow-sm border border-slate-200 print:shadow-none print:border"> <Table> <TableHeader> <TableRow className="bg-slate-100"> <TableHead className="font-bold text-slate-700">Mes</TableHead> <TableHead className="font-bold text-slate-700">Fecha</TableHead> <TableHead className="font-bold text-slate-700">Nº de Bomba</TableHead> <TableHead className="font-bold text-slate-700">Responsable</TableHead> </TableRow> </TableHeader> <TableBody> {records.map((r, i) => ( <TableRow key={i}> <TableCell className="capitalize">{r.mes}</TableCell> <TableCell>{r.fecha}</TableCell> <TableCell>{r.numero_bomba}</TableCell> <TableCell>{r.responsable}</TableCell> </TableRow> ))} </TableBody> </Table> </Card> )} </main> </div> ) }
